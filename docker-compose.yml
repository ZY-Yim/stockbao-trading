version: '3.8'

services:
  # 前端应用
  stock-trading-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    networks:
      - stock-trading-network
    restart: unless-stopped
    
  # 如果需要数据库，可以添加
  # database:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpassword
  #     MYSQL_DATABASE: stocktrading
  #     MYSQL_USER: user
  #     MYSQL_PASSWORD: password
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   networks:
  #     - stock-trading-network
  #   restart: unless-stopped

  # 如果需要 Redis 缓存
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - stock-trading-network
  #   restart: unless-stopped

networks:
  stock-trading-network:
    driver: bridge

# volumes:
#   mysql_data:
